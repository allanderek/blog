"""Placeholder. Task 4 replaces this with the real Pygments implementation.

Returning "" tells markdown-it-py to fall back to its own escaped <pre><code>,
which is the correct behaviour for an unhighlighted block.
"""
from __future__ import annotations

def highlight(code: str, lang: str, attrs: str = "") -> str:
    return ""
